Aplicar una sola tarea por vez. No marcar una tarea como completa sin ejecutar su comprobación específica. Al final de las secciones 1 y 2, detener Apply y presentar cambios, resultados y riesgos para revisión antes de continuar. Las comprobaciones identificadas como **MANUAL** requieren observación/interacción real y no pueden darse por cumplidas mediante regex, búsqueda textual ni tests estructurales.

## 1. Presentación compartida

- [ ] 1.1 Definir en una única utilidad de presentación la clasificación canónica `10..74` baja, `75..84` media y `85..100` alta, reutilizando `formatPersonalRating(...)`; comprobar con un harness focalizado `10 → 1.0/rojo`, `74 → 7.4/rojo`, `75 → 7.5/ámbar`, `84 → 8.4/ámbar`, `85 → 8.5/verde` y `100 → 10.0/verde`, además del comportamiento ante `null`.
- [ ] 1.2 Incorporar en `src/theme/colors.ts` pares semánticos reutilizables de fondo pastel y texto contrastante para los tres rangos, sin crear infraestructura dinámica de temas ni agregar dependencias; comprobar que ningún color de rating queda duplicado inline entre pantallas.
- [ ] 1.3 Crear y exportar un componente compartido equivalente a `PersonalRatingBadge` que reciba el valor canónico, muestre visualmente sólo el decimal formateado, no renderice nada para `null`, permanezca siempre fuera del árbol accesible y exponga desde la presentación compartida la descripción conceptual “Mi puntuación: X de 10” para que el contenedor principal sea la única fuente accesible.
- [ ] 1.4 Comprobar estructuralmente que el badge compartido es metadata pasiva: no contiene `Pressable`, rol de botón, handler ni focus target, y que centraliza formato, rango y apariencia en vez de duplicarlos en Biblioteca, Etiquetas o `TitleGridCard`.
- [ ] 1.5 Ejecutar `node docs/testing/personal-title-rating/section-1-verification.cjs`, el nuevo harness focalizado del badge y `npx.cmd tsc --noEmit`; registrar la salida y resolver únicamente regresiones atribuibles a esta sección.
- [ ] 1.6 Detenerse para revisión de la sección 1 y presentar API del componente, tabla de fronteras, tokens/contraste, semántica accesible, resultados de harness y typecheck antes de iniciar Integración.

## 2. Integración

- [ ] 2.1 Reemplazar en Biblioteca Detalle la presentación `Mi puntuación X/10` por el badge compartido, conservar la puntuación únicamente en el label accesible de navegación, excluir el badge visual del árbol accesible y verificar que se anuncia una sola vez y que `null` no deja badge ni hueco.
- [ ] 2.2 Extender `TitleGridCard` con una entrada opcional de datos equivalente a `personalRating?: number | null`, incorporar el rating abajo a la izquierda inmediatamente sobre el título y componer su información únicamente en el `accessibilityLabel` del `Pressable` principal; excluir el badge visual del árbol accesible y no detectar `library`, `tags` ni `search`.
- [ ] 2.3 Pasar `item.personalRating` desde Biblioteca Mosaico y verificar las cuatro combinaciones pinned/unpinned × rated/unrated, incluida la coexistencia visible de pin arriba a la derecha y rating abajo a la izquierda.
- [ ] 2.4 Reemplazar en Etiqueta abierta Detalle la presentación textual por el mismo badge compartido decorativo, incorporar la descripción compartida únicamente en el label de la navegación principal, preservar la acción contextual de pin separada y verificar anuncio único y ausencia sin hueco para `null`.
- [ ] 2.5 Pasar `item.personalRating` desde Etiqueta abierta Mosaico y verificar que usa la misma jerarquía, formato, colores y semántica que Biblioteca sin volver contextual el rating.
- [ ] 2.6 Confirmar explícitamente por API y comportamiento que Buscar Detalle y Buscar Mosaico no pasan ni muestran `personalRating`, y que `TitleGridCard` no infiere el caller.
- [ ] 2.7 Confirmar explícitamente que `TagCollage` no recibe ni muestra `personalRating` y que sus casos de póster/placeholder permanecen intactos.
- [ ] 2.8 Añadir o actualizar comprobaciones focalizadas de integración para: un único `Pressable` en `TitleGridCard`; una sola fuente accesible de puntuación por superficie; badge decorativo cuando el contenedor ya anuncia el rating; indicadores internos pasivos; pin + rating simultáneos; `null` sin espacio reservado; póster presente, ausente y con error; y ausencia de rating en Buscar y `TagCollage`.
- [ ] 2.9 Ejecutar los harnesses de integración/presentación relevantes de `docs/testing/personal-title-rating`, las nuevas regresiones focalizadas y `npx.cmd tsc --noEmit`; revisar el diff para confirmar que no cambiaron dominio, storage, sorting, editor, intent queue, `updatedAt`, `app.json` ni `package.json`.
- [ ] 2.10 Detenerse para revisión de la sección 2 y presentar matriz de las cuatro superficies incluidas, exclusiones de Search/`TagCollage`, coexistencia pin/rating, casos de imagen, navegación y resultados de typecheck antes de iniciar Validación.

## 3. Validación

- [ ] 3.1 Ejecutar el harness focalizado final y registrar los seis valores frontera exactos (`10`, `74`, `75`, `84`, `85`, `100`), sus textos y rangos, más `null`; comprobar que ninguna condición de rango está duplicada en Biblioteca, Etiquetas o `TitleGridCard`.
- [ ] 3.2 **MANUAL — móvil:** verificar Biblioteca Detalle/Mosaico y Etiqueta abierta Detalle/Mosaico con título rated/unrated, pinned/unpinned y pin + rating simultáneos; cubrir póster presente, ausente y error de imagen, confirmando posición, contraste, truncado, ausencia sin hueco y navegación de card completa.
- [ ] 3.3 **MANUAL — web y viewport angosto:** repetir la matriz visual relevante en web ancho y viewport angosto, comprobando que la pill no tapa tipo, pin ni título, que el título conserva hasta dos líneas y que no aparecen controles secundarios o cambios de layout inválidos.
- [ ] 3.4 **MANUAL — accesibilidad:** inspeccionar/recorrer con teclado y lector o herramientas accesibles la card y filas; confirmar anuncio conceptual “Mi puntuación: X de 10” una sola vez, un único focus target de navegación en Mosaico, indicadores pasivos y ausencia de nested `Pressable`.
- [ ] 3.5 **MANUAL — exclusiones y regresión:** verificar que Buscar Detalle/Mosaico y `TagCollage` siguen sin rating, que abrir títulos desde cada superficie navega al destino correcto y que las acciones contextuales de pin de Biblioteca y de cada etiqueta continúan independientes.
- [ ] 3.6 Ejecutar todos los harnesses automatizados focalizados disponibles para personal rating, browsing cards y contextual pinning que resulten aplicables; documentar por separado cualquier limitación y no sustituir con ellos las tareas manuales 3.2–3.5.
- [ ] 3.7 Ejecutar `npx.cmd tsc --noEmit` y comprobar salida exitosa con código `0` sin actualizar dependencias.
- [ ] 3.8 Ejecutar `npx.cmd openspec validate refine-personal-rating-badges --strict` y comprobar salida exitosa exacta; corregir sólo inconsistencias de este change.
- [ ] 3.9 Revisar el estado/diff final: sólo implementación y artefactos autorizados del change, ninguna modificación nueva a `app.json` o `package.json`, ninguna migración/schema/dependencia, y todos los checkboxes respaldados por evidencia real antes de solicitar archivo o commit por separado.
- [ ] 3.10 **EXTERNAL DIFF REVIEW:** identificar el commit/base real donde comienza `refine-personal-rating-badges` y revisar el diff completo de la feature usando ese boundary; no asumir automáticamente que `git diff origin/main...HEAD` representa sólo la feature. Buscar scope creep, regresiones arquitectónicas y de accesibilidad, nested `Pressable`, duplicación de lógica, cambios de dominio/storage/sorting/backup, modificaciones accidentales en `app.json` o `package.json` y cualquier cambio fuera de alcance; resolver todos los findings antes de considerar terminada la change.
- [ ] 3.11 **STOP/REVIEW:** presentar los resultados exactos de validación, los findings y resolución de la external diff review, los riesgos residuales y el estado real de todos los checkboxes; detenerse antes de proponer o ejecutar archive.

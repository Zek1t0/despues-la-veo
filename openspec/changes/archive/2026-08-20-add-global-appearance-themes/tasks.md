## 1. Theme domain, resolver and Dark + Original parity

- [x] 1.1 Inventory current shared/root/CSS/literal colors into a review fixture before migration, including navigation, rating pairs, poster overlays, danger/error foreground consumers and each disabled surface together with its actual foreground; record `consumer/file → historical value → final value → responsibility/token`, including Saved Title Detail PersonalRating persistence `ratingError` historical `#5a2a2a` → final accessible Dark `#9b7b7b` → `semantic.personalRatingErrorText`, verify the fixture matches the current repo and contains the five known static-capture modules, and do not infer danger parity from an equal PersonalRating color.
- [x] 1.2 Define typed `AppearanceScheme`, `EffectiveScheme`, palette IDs, versioned `AppearancePreference`, complete immutable `ThemeDefinition` and the minimal global/semantic/structural token groups from Design, including the palette-overridable selection trio `selectedSurface`/`selectedForeground`/`selectedBorder`, semantic `onDangerSurface`/`personalRatingErrorText` and structural `imageOverlayMedium`/`imageOverlayLabel`/`onImageOverlaySecondary`; verify TypeScript rejects incomplete definitions, semantic tokens remain palette-independent, no consumer needs palette branching, and neither global text tokens nor `onAccent` are implicit structural/selection foregrounds.
- [x] 1.3 Implement the historical six-palette baseline with complete LightBase/DarkBase plus controlled light/dark partial overrides for Original, Manzana verde, Marea, Crepúsculo de medianoche, Lavanda and Obsidiana; verify its then-current 2×6 combination set resolves to complete themes, every palette resolves an explicit contrasting `selectedForeground`, Dark + Original preserves `#ffffff`/`#0b0b0b`/`#ffffff` selection parity, and every Light combination remains light. The final catalog extension to Pinky Clouds and 2×7 is pending in Section 12 and is not represented as complete by this historical checkpoint.
- [x] 1.4 Implement pure effective-scheme and base+palette resolution, preserving palette across scheme changes and keeping System as persisted intent; add a focused Node `.cjs` harness for explicit schemes, runtime System changes, palette validation, complete composition and the documented relative-luminance Light gate (`background`, `surface`, `surfaceSecondary`, `inputBackground` each `>= 0.50`; `textPrimary` darker than `background`; basic contrast `>= 4.5:1`), including a negative `#111111`/`#eeeeee` case.
- [x] 1.5 Encode Dark + Original parity for all current intentional tokens/literals that can be checked structurally, including selected surface/foreground/border, ratings, the exact TitleGridCard `imageOverlay`/`imageOverlayMedium`/`imageOverlayStrong` trio, TagGridCard `imageOverlayLabel rgba(11, 11, 11, 0.94)` with name `onImageOverlay #f2f2f2` and count `onImageOverlaySecondary #bdbdbd`, `#f4b8b8` standalone `dangerText`, `#f2f2f2` `onDangerSurface`, Saved Title Detail PersonalRating persistence `ratingError` historical `personalRatingErrorText #5a2a2a` together with its authorized Section 12 final Dark `#9b7b7b`, and the real foregrounds paired with disabled `#303030/#3b3b3b`; verify the concrete consumer/token mapping, exact consumer-specific values and the Section 12 accessibility boundary instead of silently normalizing Dark + Original or inflating the token catalog.
- [x] 1.6 Run the historical Section 1 harnesses and `npx tsc --noEmit`, confirm the then-current 12 complete themes pass the selection-pair and Light-luminance gates, inspect the diff for an inflated token catalog or duplicated complete themes, then STOP for selective staging, real diff, external review and a dedicated checkpoint commit/push before Section 2. The final contract requires those assumptions to become seven palettes and 14 complete themes during the single pending Pinky Clouds Apply.

## 2. Appearance persistence, concurrency, provider and hydration

- [x] 2.1 Implement strict parse/serialize/default behavior for one versioned Appearance JSON payload stored under the stable key `appearance`, with default Dark + Original; add harness cases for absence, valid values, malformed JSON, invalid scheme, unknown palette, wrong payload version, future payload migration boundary and extra data policy.
- [x] 2.2 Add an Appearance-specific `app_preferences` repository without reusing `viewPreferencesRepo`, changing `SavedTitle` or raising SQLite schema v3; implement a queue/transaction-free `setAppearancePreferenceWithDb(db, ...)` helper and verify round-trip, `updated_at`, read-error distinction and idempotent use of the existing table.
- [x] 2.3 Implement the public Appearance mutation with exactly `initDb()` → one `runSerializedStorageMutation` entry → one `db.withTransactionAsync` → `setAppearancePreferenceWithDb`; add structural/runtime checks proving the helper does not call `initDb`, queue, transaction or public setter, a rejection does not break the global queue, and other title/pin/backup mutation semantics remain unchanged.
- [x] 2.4 Implement the central serialized/coalescing intent coordinator with monotonic IDs and separate `latestIntent`/`displayed`/`confirmedPersisted`; extend that same coordinator in Section 11 with typed opaque deferred handles and minimal reserve/activate/discard operations that share `select()`'s monotonic sequence, retain the reserved order during activation and never create caller-side or backup-specific precedence; within that one coordinator/sequence distinguish the deferred causal activation watermark from the normal displayed/write lifecycle so an unactivated reservation alone cannot coalesce or suppress a prior visible normal write; verify it chooses logical writes but delegates every real write to the public queue-compliant setter, every successful write updates `confirmedPersisted` even if superseded, and an applicable normal failure rolls back exactly to current `confirmedPersisted`.
- [x] 2.5 Add a pure coordinator harness for confirmed C → write A success → latest write B failure, proving displayed rollback, storage, `confirmedPersisted` and restart all resolve to A; retain explicit cases for coalesced-before-write, superseded failure, latest success, late success, multiple rapid intents, queue after rejection and retry, and in Section 11 extend the harness with reserve-without-display/confirmation/write, activation with the original order, supersession by normal selection or later reservation, discard without rewind, a prior normal queued write not coalesced solely by reservation, prior normal success/failure reconciliation while reservation is pending, activation failure rollback and all prior hydration/retry/concurrency regressions.
- [x] 2.6 Add `AppThemeProvider` and `useAppTheme()` exposing hydration/storage availability, effective scheme/theme, `confirmedPersisted` and scheme/palette intents with memoized values; verify no palette-specific conditional is required in screens/components.
- [x] 2.7 Subscribe reactively to runtime system scheme on Android/iOS/web semantics and recompute only when preference is System; verify explicit Light/Dark ignore system changes and System changes do not enqueue writes.
- [x] 2.8 Add the short Dark + Original hydration gate and generation/intent-revision guard for hydration/retry reads; verify stored Light+Marea, no row, invalid row, thrown initialization, unmount/late completion, and that normal reads do not enter the mutation queue.
- [x] 2.9 Add the explicit stale-retry harness: initial read error → retry begins and observes old A → user selects B → B write succeeds → retry A arrives late; prove `displayed`, `confirmedPersisted`, storage and restart remain B. Also cover pending retry → newer write fails → rollback uses live `confirmedPersisted`, never the retry snapshot.
- [x] 2.10 Run all Section 2 harnesses and `npx tsc --noEmit`, confirm a single queue entry/transaction, queue recovery after rejection, untouched browsing/TMDB contracts and unchanged other mutation semantics, then STOP for selective staging, real diff, external review and a dedicated checkpoint commit/push before Section 3.

## 3. Root Navigation, Tabs, StatusBar and web bootstrap

- [x] 3.1 Integrate `AppThemeProvider` at root and derive React Navigation's complete theme from the effective ThemeDefinition, removing the private root dark palette; verify Settings headers and Stack content share the current effective background/text/border/accent.
- [x] 3.2 Migrate Tabs header, bar, active/inactive labels/icons and borders to the same reactive source while retaining filled/outline active signals; verify runtime palette and scheme changes leave no stale tab surface.
- [x] 3.3 Derive StatusBar content style from effective scheme and migrate explicit title/TMDB detail header overrides; verify Light produces dark StatusBar content and no light screen/dark stale header combination.
- [x] 3.4 Inspect and record the protected `app.json`/`package.json` diffs, install exclusively the Expo SDK 54-compatible `expo-system-ui`, keep `userInterfaceStyle: "automatic"`, and register its config plugin only if the real Expo/CNG configuration requires it; verify Expo config/introspection enables Android native System behavior, preserve `android.package` and personal package scripts exactly, keep lockfile changes attributable only to this dependency, and prepare later selective staging of feature-owned hunks without staging personal changes.
- [x] 3.5 Convert `global.css` to Dark + Original bootstrap fallbacks plus runtime-fed CSS variables for root background/text, scrollbars, focus and `color-scheme`; verify palette tables are not duplicated in CSS.
- [x] 3.6 Add a small web synchronization effect for document/root variables and browser scheme, with cleanup/SSR-safe guards; verify reload, System runtime change, React/DOM agreement and visible keyboard focus.
- [x] 3.7 Run focused resolver/provider checks, Expo config/introspection, `npx tsc --noEmit` and manual web reload/theme switching; on a physical Android build verify System follows real runtime light/dark changes while persisted scheme/palette remain unchanged and `useColorScheme` stays the sole React runtime source. Review that `app.json`/`package.json` personal hunks are preserved and only `expo-system-ui` plus its attributable lockfile/plugin hunks were added, then continue directly with Section 4 without an intermediate checkpoint; perform selective staging, real diff, external review and the combined Sections 3 + 4 checkpoint only after Section 4 and before Section 5.

## 4. Settings integration, Appearance route and real previews

- [x] 4.1 Add `/settings/appearance` as a normal Stack route and a clear entry from the existing Settings screen without changing other navigation; verify back navigation, header theme and TMDB/About entries remain unchanged.
- [x] 4.2 Build accessible Scheme controls labelled `Del sistema`, `Claro`, `Oscuro` with immediate optimistic selection, selected state, non-color indicator and recoverable persistence feedback; verify there is no Apply button.
- [x] 4.3 Build palette catalog metadata with stable IDs and approved Spanish display names, preserving the existing display label `Obsidiana`; verify catalog order/IDs match parser and backup contracts.
- [x] 4.4 Implement abstract palette previews from the same real resolver/ThemeDefinition used by the app, showing background, primary/secondary surfaces, text, accent and border/selection; add a structural check that no preview-only palette color table exists.
- [x] 4.5 Implement mobile horizontal browsing and measured responsive wide-layout wrapping/grid behavior without freezing an unsupported breakpoint; verify narrow viewport, wide web, resize and rotation.
- [x] 4.6 Add keyboard/focus, labels, checks/icons, selected border/indicator and `accessibilityState.selected` for scheme/palette controls; verify screen-reader order and keyboard operation on web.
- [x] 4.7 Exercise rapid selections and the real confirmed C → A success → B failure sequence through the screen, run `npx tsc --noEmit`, manually validate narrow/wide web, then STOP for selective staging, real diff, external review and a dedicated checkpoint commit/push before Section 5.

## 5. Shared components, static captures and semantic overlays

- [x] 5.1 Migrate `LayoutOption.tsx` colors out of module-load `StyleSheet.create` while retaining layout/typography statically; verify selected/unselected/pressed states react in runtime themes.
- [x] 5.2 Migrate `PosterPlaceholder.tsx` and `TagCollage.tsx` surfaces, borders and icons to runtime theme fragments; verify placeholder visibility in Light/Dark and collage layout parity.
- [x] 5.3 Migrate `TitleGridCard.tsx` global card tokens while formalizing dark structural `imageOverlay`/`imageOverlayMedium`/`imageOverlayStrong`, `onImageOverlay` and overlay border; verify the exact Dark + Original `0.78` title, `0.82` type-badge and `0.90` pin scrims and that arbitrary light-theme posters remain readable without palette overrides.
- [x] 5.4 Preserve the passive top-right diamond-outline contextual pin and rating/pin coexistence across narrow cards/long titles; verify no new Pressable, handler, domain change or overlap is introduced.
- [x] 5.5 Migrate `ViewOptionsPanel.tsx` panel/selection colors while keeping modal backdrop structural and static geometry unchanged; verify panel, options and backdrop react without stale captures.
- [x] 5.6 Migrate `PersonalRatingBadge` to scheme-resolved semantic pairs while preserving `10..74`, `75..84`, `85..100`, numeric/accessibility labels and passive behavior; verify palettes cannot remap rating semantics.
- [x] 5.7 Search all `StyleSheet.create`, color literals and theme imports for remaining module-load captures, run shared-component harnesses and `npx tsc --noEmit`, visually compare Dark + Original, then STOP for selective staging, real diff, external review and a dedicated checkpoint commit/push before Section 6.

## 6. Biblioteca

- [x] 6.1 Migrate Biblioteca list/grid, search-within-library, filters, options, loading/empty/error/destructive states and contextual pin controls to semantic runtime tokens, using `dangerText` for standalone feedback and `onDangerSurface` for content placed on `dangerSurface`; verify Dark + Original foreground parity and that SavedTitle, sorting, viewMode and pin behavior are unchanged in both layouts.
- [x] 6.2 Run Biblioteca-focused harnesses, `npx tsc --noEmit` and manual checks for detail/grid, filters, search, empty/error/loading, pin/rating coexistence and Dark + Original parity.
- [x] 6.3 Search the Biblioteca diff for old theme imports, literals, static captures or domain/sorting/viewMode changes, then STOP for selective staging, real diff, external review and a dedicated checkpoint commit/push before Section 7.

## 7. Buscar

- [x] 7.1 Migrate Buscar inputs, placeholder, actions, result detail/grid, loading/empty/error/TMDB credential states to semantic runtime tokens; verify debounce, remote request identity, natural ordering and credential behavior are unchanged.
- [x] 7.2 Run Search-focused harnesses, `npx tsc --noEmit` and manual checks for detail/grid, query transitions, credential absent/error/configured paths, placeholders, focus and Dark + Original parity.
- [x] 7.3 Inspect the Buscar diff for stale colors or behavioral changes, then STOP for selective staging, real diff, external review and a dedicated checkpoint commit/push before Section 8.

## 8. Etiquetas

- [x] 8.1 Migrate Etiquetas list/grid, collage labels, search, selected-tag detail, options and contextual pin controls to runtime tokens, resolving the TagGridCard name/count scrim specifically through palette-independent `structural.imageOverlayLabel`, its name through `structural.onImageOverlay` and its count through `structural.onImageOverlaySecondary`; verify exact Dark + Original `rgba(11, 11, 11, 0.94)`/`#f2f2f2`/`#bdbdbd`, unchanged position/padding/typography/counter/navigation/poster layout/press target, and unchanged sorting, tag navigation, viewMode and pin contexts.
- [x] 8.2 Run Tags-focused harnesses, `npx tsc --noEmit` and manual checks for list/grid, the concrete TagGridCard `imageOverlayLabel`/`onImageOverlay`/`onImageOverlaySecondary` mappings under Dark/Light palettes, selected-tag results, search, empty/error/loading, pin/rating coexistence and Dark + Original parity; verify palettes cannot override structural tokens and no additional overlay or foreground level was introduced.
- [x] 8.3 Inspect the Etiquetas diff for stale captures, literals or domain changes, then STOP for selective staging, real diff, external review and a dedicated checkpoint commit/push before Section 9.

## 9. Saved Title Detail and TMDB Remote Detail

- [x] 9.1 Migrate saved title detail surfaces, selections, inputs/placeholders, notes, rating controls, pin, errors and destructive actions, mapping PersonalRating persistence `ratingError` exclusively to `theme.semantic.personalRatingErrorText`; preserve the historical Dark + Original `#5a2a2a` through the initial migration, then apply only the explicitly authorized Section 12 accessible Dark value `#9b7b7b`, and verify PersonalRating `10..100 integer | null`, queue/persistence, optimistic behavior, rollback/error lifecycle, notes draft/autosave, tags, pins, layout, wording and header coherence remain unchanged.
- [x] 9.2 Migrate TMDB remote detail surfaces, posters/placeholders, providers, actions, loading/errors and disabled state; verify save/open/detail/credential/remote behavior is unchanged.
- [x] 9.3 Run detail-focused harnesses, `npx tsc --noEmit` and manual checks for headers/content, form states, rating ranges, pin, destructive actions, remote providers/errors, Dark + Original and runtime Light/palette changes.
- [x] 9.4 Inspect the two-detail-screen diff for stale colors or functional changes, then STOP for selective staging, real diff, external review and a dedicated checkpoint commit/push before Section 10.

## 10. Settings main, TMDB Settings and About/Credits

- [x] 10.1 Migrate Settings main and its progress/disabled/error/action surfaces while preserving backup navigation and behavior.
- [x] 10.2 Migrate TMDB credential settings inputs, placeholders, show/hide, disabled, feedback, danger and link/focus states; verify credential lifecycle/storage/request behavior is unchanged.
- [x] 10.3 Migrate About/Credits surfaces and links while preserving the exact TMDB notice, exact JustWatch attribution and untinted/unfiltered `tmdb-primary-full-blue.png`; validate logo contrast through its surrounding surface only.
- [x] 10.4 Run Settings/TMDB/About-focused checks and `npx tsc --noEmit`; manually verify keyboard/focus, links, credential errors, branding and Dark + Original plus representative Light palettes.
- [x] 10.5 Search all migrated app/src consumers for old `colors` imports and significant themeable literals, inspect the Section 10 diff for behavioral/branding changes, then STOP for selective staging, real diff, external review and a dedicated checkpoint commit/push before Section 11.

## 11. Backup v4, portable Appearance and compatibility

- [x] 11.1 Define pure backup v4 types and creator extending v3 with optional `appearance`, including only trustworthy `confirmedPersisted { scheme, palette }`; verify output excludes effective/system scheme, resolved tokens and browsing preferences.
- [x] 11.2 Implement v4 parsing that preserves current item/pin validation and returns Appearance as valid, absent or incompatible; verify missing Appearance and unknown palette never make a processable envelope fatal.
- [x] 11.3 Extend the common parser to accept v4 while retaining exact v1/v2/v3 behavior and treating their missing Appearance as preserve-local; run all historical fixtures plus explicit local-Appearance-preservation cases.
- [x] 11.4 Extend export snapshot/orchestration with explicit availability states: known `confirmedPersisted` includes its real value; successful no-row includes contractual Dark + Original; invalid row or read/storage error without trustworthy confirmation omits Appearance while continuing items/pins export. Verify pending/failed optimistic UI and visual fallback are never exported as confirmed, and a normal B write that succeeds while deferred A is pending updates the export source to B rather than C or A.
- [x] 11.5 Add the explicit read-error round-trip harness: Appearance read throws → v4 data export succeeds without `appearance` → import restores eligible items/pins → destination Appearance remains unchanged; verify omission may be reported without blocking export.
- [x] 11.6 Extend the existing AppearanceCoordinator with minimal typed opaque `reserveDeferred`/`activateDeferred`/`discardDeferred`-equivalent operations: reserve a valid v4 Appearance at import confirmation using the same monotonic sequence as normal `select()`, without changing `displayed`/`confirmedPersisted` or writing; keep an unactivated reservation out of the active displayed/write lifecycle so it cannot alone coalesce a prior normal write or suppress that write's success/failure rollback; retain the original reserved order on activation, and discard without rewinding, recycling IDs or changing the real normal-write outcome reached while pending.
- [x] 11.7 Add the required late-merge harness: confirm import A → main merge pending → user selects/writes B → merge succeeds late; prove items/pins import succeeds while imported A is discarded and final `displayed`, storage, `confirmedPersisted` and restart remain B.
- [x] 11.8 After the main backup mutation has fully exited its existing queue entry/transaction, activate the opaque deferred handle through the coordinator; if still latest, persist through a fresh call to the public Appearance setter while preserving the reserved order, and add structural instrumentation/checks proving reserve/discard do not write, no nested queue or transaction exists, and activation creates exactly one Appearance queue entry/transaction.
- [x] 11.9 Report Appearance application success, supersession, absence, incompatibility or persistence failure separately from item/pin results; verify valid library + unknown palette, newer user intent and v4 without Appearance all preserve the correct local Appearance.
- [x] 11.10 Add focused Node `.cjs` coverage for v4 present/absent round-trip, v1/v2/v3 compatibility, unknown palette, invalid scheme, partial item/pin outcomes, cancel/rejected/thrown merge, Appearance write failure, repeated import and export from actual `confirmedPersisted` after superseded-success/latest-failure; pure coordinator cases must prove reserve changes neither `displayed` nor `confirmedPersisted` and performs no write, applicable activation preserves the reserved order, newer `select()` or reservation supersedes without write, discard prevents activation without state change or ID reuse, repeated A→D ordering, activation failure uses existing rollback, the queue survives rejection, and all previous hydration/retry/concurrency cases still pass. Explicitly cover: select B queued → reserve A does not coalesce B; pending B → reserve A → B success yields B/B/B and discard preserves B; pending B → reserve A → B failure rolls back to C/C/C and discard preserves C; B success → pending A exports B; B success → reserve A → applicable activation may apply A with its reserved order; reserve A → later select D still supersedes A.
- [x] 11.11 Update Settings export/import confirmation/result copy for v1-v4 without changing merge semantics, run all backup/storage harnesses and `npx tsc --noEmit`, inspect the data-safety and queue-boundary diff, then STOP for selective staging, real diff, external review and a dedicated checkpoint commit/push before Section 12.

## 12. Accessibility, Android/iOS/web matrix and final regression

- [x] 12.1 Preserve the completed historical Light/Dark × six-palette audit, then extend the final contract to Light/Dark × seven palettes = 14 `ThemeDefinition` values for text/surfaces, accents/onAccent, `selectedSurface`/`selectedForeground`/`selectedBorder`, borders, inputs/placeholders, disabled, danger, rating pairs, links, pins, overlays and focus. This literal `[x]` records only the clean 2×6 audit completed against the catalog that existed at that time; it MUST NOT be read as completion of the pending final 2×7 regression. The final regression MUST preserve the current user-authored surface/input refinements for Manzana verde, Marea and Crepúsculo de medianoche documented in Design, including Manzana verde Light `borderStrong #5a7b5a`, while changing only the three failing manual Dark boundaries: Manzana verde `#202920 → #646464`, Marea `#304144 → #4f6c72`, and Crepúsculo `#454864 → #606485`. It MUST audit all 14 current definitions against their real surfaces, update obsolete harness expectations without reducing coverage, preserve Pinky Clouds exactly, and preserve the documented PersonalRating persistence correction `personalRatingErrorText` Dark `#5a2a2a` → palette-independent `#9b7b7b` plus Light `#7d2020` and the Light Crepúsculo `textMuted #6e6e6e` override. Add no input-specific token or consumer branch. Pinky Clouds Light MUST keep `accent #AA4275` separately from `borderStrong #B24A7D`; its real `accent/surface` link pair is `4.7044:1`, while the synthetic Light `accent/surfaceSecondary` pair MUST NOT be treated as a text failure absent a real normal-text consumer. Continue to STOP on any additional consumer-proven failure rather than silently changing colors.

The following Pinky Clouds work remains pending despite the historical checkboxes above and MUST be performed in one final Apply without changing those checkbox markers:

- [x] **12.P1 — domain and resolver:** add stable `AppearancePaletteId` `pinky-clouds`, label `Pinky Clouds`, catalog position 7 and exact Light/Dark partial overrides from Design; resolve 14 complete themes through the existing base + palette system, with no token addition, duplicate theme system or palette branch in consumers.
- [x] **12.P2 — persistence and backup:** keep Appearance payload `{ version: 1, scheme, palette }`, default Dark + Original and backup `version: 4`; prove persistence hydration plus v4 export/import/restore of `pinky-clouds`, and replace any unknown-palette fixture that used `pinky-clouds` with another deliberately invalid ID. v1/v2/v3 imports continue preserving local Appearance and concurrency/hydration/coordinator semantics remain unchanged.
- [x] **12.P3 — preview, web and structure:** add Pinky Clouds only through the catalog/resolver so its preview and browser chrome derive from the same `ThemeDefinition`; keep `global.css` as bootstrap Dark + Original, add no Pinky-specific CSS/table and change selector logic only by catalog expansion.
- [x] **12.P4 — automated regression:** update every obsolete harness assumption to the current seven-palette/14-definition contract, including the final Manzana verde/Marea/Crepúsculo values and three Dark `borderStrong` corrections documented in Design; retain specific coverage for Pinky Clouds Light/Dark, catalog order, resolver completeness, persistence, backup v4, real preview derivation, full consumer-driven accessibility and structural absence of consumer branches/duplicate CSS or preview palettes. Preserve all existing 26 harnesses and do not reduce coverage.
- [x] **12.P5 — final manual validation:** validate Pinky Clouds Light and Dark on representative Appearance preview, Biblioteca cards, Search, Details, Settings and inputs. A representative matrix MAY establish identity without traversing the entire app twice exclusively for Pinky Clouds. This is the final product round; no further palette expansion follows.
- [x] **12.P6 — final gate:** after implementing the three approved Dark `borderStrong` fixes and synchronizing the harness with all current manual palette values, run the updated 14-theme automatic regression, final manual validation and a renewed external review of the accumulated Section 12 technical diff, then STOP for the checkpoint. The prior review remains incomplete until this finding is implemented and revalidated; do not perform another product-design round.
- [x] 12.2 Manually validate Android physical device across all schemes/palettes, runtime System changes, restart/hydration, rapid selections including A-success/B-failure, resize/rotation where applicable, StatusBar, pin/rating coexistence and operation without TMDB credential.
- [x] 12.3 Manually validate iOS physical device across the same matrix, including splash/gate, runtime System changes, safe areas, StatusBar, keyboard inputs and persistence.
- [x] 12.4 Manually validate web reload/first paint, all schemes/palettes, runtime `prefers-color-scheme`, DOM/browser surfaces, scrollbars, keyboard/focus, narrow/wide resize and responsive previews.
- [x] 12.5 Manually validate Biblioteca detail/grid, Search, Tags, both detail screens, Settings, credential settings and About; confirm exact branding copy/asset and unchanged functional navigation/domain behavior.
- [x] 12.6 Manually validate export/import v4 with Appearance present and absent, v1/v2/v3 fixtures, unknown-palette result, cancelled/rejected restore, import pending followed by newer user selection, restart after imported Appearance and local-first behavior with no network/credential.
- [x] 12.7 Simulate hydration/read error, stale retry followed by newer successful/failed writes, superseded success followed by latest persistence failure, global queue rejection/recovery and Appearance backup-write failure; verify truthful fallback/rollback/export behavior and intact library/pins/preferences.
- [x] 12.8 Run every focused `.cjs` harness, `npx tsc --noEmit` and `npx.cmd openspec validate add-global-appearance-themes --strict`; record exact results and resolve failures within scope.
- [x] 12.9 Perform a final structural search for palette-specific branches in screens, duplicate preview/CSS palettes, old theme imports, static color captures, any dependency/config change beyond the approved Expo SDK 54-compatible `expo-system-ui` integration, and modified browsing/TMDB/domain behavior.
- [x] 12.10 Conduct the final external feature diff review against the pre-feature baseline, with special attention to preserving personal `android.package` and package-script hunks while selectively staging only feature-owned `userInterfaceStyle`, approved `expo-system-ui`, attributable lockfile and required plugin changes, plus Dark + Original parity, backup compatibility/data safety, accessibility and scope exclusions; STOP for approval and a dedicated final checkpoint commit/push before Archive.

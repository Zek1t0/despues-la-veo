Implementar una sola sección lógica por vez: dentro de esa sección completar sus tareas, ejecutar los harnesses, regresiones, `npx.cmd tsc --noEmit` y validación OpenSpec indicados, detenerse en STOP / REVIEW y esperar aprobación externa antes de comenzar la sección siguiente. No marcar una tarea como completa sin su comprobación específica. Los checks **MANUAL** requieren observación o interacción real y no pueden sustituirse con regex, lectura estructural ni mocks. El checkpoint especial 2.2 exige aprobación antes de instalar dependencias.

## 1. Credential service, errores y seam del cliente

- [x] 1.1 Definir el contrato puro e inyectable de `TmdbCredentialStore`, los estados públicos de credencial y la normalización focalizada (`trim`, un único prefijo `Bearer ` tolerado, vacío inválido, ninguna otra transformación); comprobar con un harness los casos exactos de whitespace, prefijo, token intacto y candidato vacío.
- [x] 1.2 Implementar el modelo tipado mínimo `credential-not-configured`, `credential-storage-error`, `credential-invalid`, `network`, `aborted`, `rate-limited`, `http` e `invalid-response`, incluyendo status/código remoto sólo cuando sean seguros; comprobar por separado ausencia confirmada, read/init/write/delete failure, 401, 429, 5xx, red, AbortError y respuesta inválida.
- [x] 1.3 Implementar `TmdbCredentialService` puro contra store y validator inyectables, con inicialización lazy compartida, cache, suscripción no React, snapshot atómico y operación retry/reinitialize: una promise fallida se libera, cada retry crea una nueva lectura compartida y retries concurrentes reutilizan sólo esa lectura; comprobar first-get-throws→retry-token→configured, first-get-throws→retry-null→not-configured, retry-fails→storage-error reintentable, zero TMDB requests mientras espera y generation sin incremento por hidratación.
- [x] 1.4 Implementar mutations serializadas y gate de delete sin deadlock: requests nuevos esperan la misma mutation; comprobar carrera A (configured A→delete pending→R espera sin fetch→success→not-configured+generation una vez→R recibe not-configured+zero fetch) y carrera B (configured A→delete pending→R espera→failure→caller storage-error→snapshot/cache/generation intactos→R continúa con A), además de token anterior ante validation/write fallido.
- [x] 1.5 Extraer un transporte HTTP explícito que recibe token no vacío, path y opciones, y concentra base URL, Authorization, fetch, AbortSignal, clasificación y parsing seguro sin importar service/store/React/screens; comprobar Bearer exacto, 401/429/5xx/network/abort/invalid-response y que el transporte nunca resuelve credencial global.
- [x] 1.6 Implementar un validator inyectable que use el transporte para `GET /authentication`; comprobar candidato vacío→zero fetch y el caso negativo A/B: con A configurado, validar B usa exclusivamente B, no lee/inicializa store, no usa A, no persiste, no cambia cache/status/generation y conserva A hasta save validado+exitoso.
- [x] 1.7 Añadir harnesses negativos con token canario que demuestren ausencia del token íntegro y de cada fingerprint/prefijo/sufijo/substring deliberado o representación derivada implementada, y que errores/logs sanitizados nunca reciben Authorization, URLs/query params con token ni raw body; documentar que no se pretende probar coincidencias accidentales arbitrarias.
- [x] 1.8 Mantener en esta sección el wiring runtime existente compilable: no conectar todavía `tmdbClient` al nuevo service, no eliminar aún `EXPO_PUBLIC_TMDB_TOKEN` y no introducir adapter ficticio/fallback temporal; comprobar que los módulos preparatorios se ejercitan sólo con doubles y no son importados por producción antes de Section 2.
- [x] 1.9 Ejecutar todos los harnesses de la sección, regresiones existentes del provider que resulten aplicables, `npx.cmd tsc --noEmit` y `npx.cmd openspec validate add-user-tmdb-credential --strict`; corregir sólo problemas atribuibles a esta sección.
- [x] 1.10 **STOP / REVIEW:** presentar boundary acíclico, lifecycle de initializationPromise/retry, carreras concurrentes de retry y delete, tabla storage-error, semántica de generation, evidencia A/B/zero-fetch/no-leakage y runtime vigente compilable antes de instalar dependencies/adapters.

## 2. Adapters native/web y dependencia protegida

- [x] 2.1 Registrar antes de cualquier instalación `git status`, diff y hashes de `app.json` y `package.json`, identificar el lockfile real y separar cambios locales preexistentes del usuario de los requeridos por esta feature; presentar la estrategia exacta de preservación y detenerse si una modificación no puede aislarse sin revertir, reemplazar o formatear contenido protegido.
- [x] 2.2 **STOP / REVIEW DE DEPENDENCIA:** obtener aprobación sobre la línea base y estrategia; no ejecutar instalación ni editar configuración antes de este checkpoint.
- [x] 2.3 Instalar exclusivamente `expo-secure-store` mediante `npx.cmd expo install expo-secure-store` usando la versión compatible con el Expo SDK real del repo; inspeccionar inmediatamente cambios en `package.json`, lockfile y `app.json`, preservar los cambios del usuario y detenerse si la herramienta introduce rewrites o dependencias no autorizadas.
- [x] 2.4 Configurar sólo el plugin/opciones realmente requeridos por la versión instalada, sin biometría obligatoria ni cambios masivos; comprobar documentación/versiones instaladas y revisar el diff focalizado de `app.json`, `package.json` y lockfile.
- [x] 2.5 Implementar el adapter native con `expo-secure-store`, clave estable y operaciones async get/set/delete sin `requireAuthentication`; comprobar el contrato mediante boundary inyectable/mock sin simular como MANUAL las garantías reales de Keychain/Keystore.
- [x] 2.6 Implementar el adapter web con `localStorage` namespaced, persistencia entre recreaciones del adapter y errores explícitos de get/set/delete cuando storage está bloqueado o lanza; comprobar que no cae a memoria, SQLite, `app_preferences` ni `sessionStorage`.
- [x] 2.7 Integrar selección de adapter, singleton service+validator y provider/hook React no bloqueante, incluida la acción real de retry/reinitialize; comprobar snapshots initializing/configured/not-configured/storage-error, nueva lectura tras fallo, retries concurrentes deduplicados y rutas locales renderizando durante cada intento.
- [x] 2.8 Con la composición real disponible, conectar `tmdbClient` runtime para obtener una snapshot del service y pasar el token al transporte explícito; mapear read/init failure a `credential-storage-error`, ausencia confirmada a `credential-not-configured` y comprobar snapshot old/new por request.
- [x] 2.9 Eliminar definitivamente la lectura de `EXPO_PUBLIC_TMDB_TOKEN` y cualquier fallback/adapter temporal del runtime; comprobar por búsqueda global y harness que todos los requests autenticados normales usan service→snapshot→explicit-token transport y que validation no recorre ese resolver.
- [x] 2.10 Añadir harnesses estructurales que demuestren que database/schema/preferences/backups no importan el credential store ni contienen su key/token, que adapters no importan biblioteca y que el grafo service→validator→transport / client→service→transport no contiene ciclo de módulos.
- [x] 2.11 Ejecutar harnesses de adapters/service/client, regresiones de SQLite/backup/preferences/provider, `npx.cmd tsc --noEmit` y OpenSpec strict; verificar zero fallback, schema/version de SQLite y backup sin cambios.
- [x] 2.12 **STOP / REVIEW:** presentar versión/config SecureStore, diff protegido, composición runtime, eliminación final del env, boundary acíclico, storage-error, exclusiones SQLite/backup y resultados antes de construir UI.

## 3. Ajustes y screen de configuración

- [x] 3.1 Incorporar en Ajustes una sección TMDB que distinga initializing, not-configured, configured y storage-error, y conectar `Reintentar` a una reinitialization real; comprobar retry→token, retry→null, retry→nuevo fallo y múltiples activaciones concurrentes sin lecturas duplicadas.
- [x] 3.2 Crear una screen propia de configuración y su navegación, no modal, con explicación, campo de API Read Access Token, show/hide accesible, candidato local, `Obtener token` y `Guardar y comprobar`; comprobar que cambiar comienza con campo vacío y nunca precarga el token guardado.
- [x] 3.3 Conectar validación/save/change para que 200 persista, 401 marque inválido, network/timeout/5xx mantenga candidato y write failure muestre `credential-storage-error`; comprobar retry, submits concurrentes, token anterior y generation sin cambio ante fallos.
- [x] 3.4 Implementar eliminación con confirmación, estado pendiente, éxito `No configurado` y `credential-storage-error` ante fallo con rollback coherente; comprobar que no hay request nuevo durante delete y que el fallo no publica generation permanente ni falsa ausencia.
- [x] 3.5 Añadir en web la advertencia explícita sobre almacenamiento en navegador, ausencia de protección Keychain/Keystore/SecureStore, borrado por limpieza de datos y acceso potencial de JavaScript same-origin; comprobar copy visible sin promesas de cifrado.
- [x] 3.6 Implementar el link `Obtener token` al flujo oficial apropiado de TMDB y fallo amistoso si no puede abrirse; revisar que la UI use `API Read Access Token de TMDB` y evite copy técnica innecesaria.
- [x] 3.7 Añadir harnesses focalizados de estado/configuración, accesibilidad básica, show/hide, token anterior preservado, no revelación del token configurado y separación respecto de backup.
- [x] 3.8 Ejecutar harnesses de la sección, regresiones de Ajustes/import/export, `npx.cmd tsc --noEmit` y OpenSpec strict.
- [x] 3.9 **STOP / REVIEW:** presentar matriz completa de configuración/cambio/eliminación, copy web, links, accesibilidad, no-leakage y resultados antes de integrar Search/detalle.

## 4. Search y detalle remoto

- [ ] 4.1 Integrar el snapshot en Search: durante initializing/retry no buscar; en not-configured conservar query y mostrar CTAs; en storage-error hacer zero requests y conectar Reintentar real; comprobar `initializing→configured` y `storage-error→retry→configured` ejecutan la query pendiente exactamente una vez aunque generation no cambie, mientras retry→null/failure no busca.
- [ ] 4.2 Implementar deduplicación por transición/identidad coherente sin imponer mecanismo React: disparar una vez por condición no usable→configured, o por configured→configured con generation nueva tras replacement; comprobar que save desde not-configured no dispara dos veces aunque status y generation cambien juntos, y que delete exitoso sólo invalida resultados.
- [ ] 4.3 Traducir en Search `credential-invalid` a CTA Cambiar, network/rate-limit/http recuperable a Reintentar y `aborted` a salida silenciosa; comprobar zero search requests sin token y ausencia de errores genéricos incorrectos.
- [ ] 4.4 Adaptar el detalle remoto para estados amistosos absent/invalid/network/http sin referencias a `.env` ni bodies técnicos, conservando sin refactor el `Promise.all` atómico de detalles, créditos y proveedores y bloqueando save/update cuando no hay metadata cargada.
- [ ] 4.5 Añadir harnesses de Search para startup hydration→configured+query una vez+generation unchanged, storage-error→retry→configured+query una vez, replacement configured→configured+generation nueva+query una vez, save absent→configured sin doble disparo, delete success sólo invalida, y cero búsqueda adicional por validation failure, candidate 401, network/timeout, retry failure/null, write/delete failure, snapshot/render repetido; conservar carreras delete pending y detalle remoto.
- [ ] 4.6 Ejecutar regresiones de browsing cards, Search views, SavedTitle re-save y provider, más `npx.cmd tsc --noEmit` y OpenSpec strict; comprobar que no cambiaron sorting, views, rating, pins ni metadata personal.
- [ ] 4.7 **STOP / REVIEW:** presentar máquina de estados de Search, conteos de requests, transiciones no-token→configured→removed, manejo del detalle y evidencia de carga atómica preservada antes de attribution.

## 5. Attribution y Créditos

- [ ] 5.1 Identificar desde fuentes oficiales el logo TMDB aprobado y las reglas vigentes de uso; registrar procedencia/licencia aplicable y detenerse si no puede verificarse que el asset elegido es oficial y permitido.
- [ ] 5.2 Incorporar el asset oficial sin redibujar, recolorear, deformar ni hacerlo más prominente que la marca de la app; comprobar dimensiones, proporción, contraste y atribución del archivo.
- [ ] 5.3 Crear una screen o sección separada `Acerca de / Créditos` enlazada desde Ajustes con el notice exacto `This product uses the TMDB API but is not endorsed or certified by TMDB.`, link a TMDB e indicación de servicio externo.
- [ ] 5.4 Comprobar estructuralmente que Credits no importa credential service/store, no muestra token/status y no crea infraestructura legal general.
- [ ] 5.5 Ejecutar harnesses/validaciones de contenido y assets, `npx.cmd tsc --noEmit` y OpenSpec strict.
- [ ] 5.6 **STOP / REVIEW:** presentar fuente y uso del logo, screen final, notice exacto, links, accesibilidad y resultados antes de validación final.

## 6. Validación integral, manual y revisión externa

- [ ] 6.1 Ejecutar la suite completa service/client/transport/Search: init compartida y startup token→query una vez sin generation; retry token/null/failure y retries concurrentes; configured replacement→query una vez por generation; eventos negativos sin búsqueda; absent/storage-error; A/B override; errores/mutations; y ambas carreras delete+request sin deadlock ni fetch indebido.
- [ ] 6.2 Ejecutar la suite de seguridad/storage: token canario íntegro y representaciones deliberadas implementadas ausentes de mensajes/errores/logs/URLs/query params, Authorization/raw bodies nunca entregados a logging, backup/SQLite sin storage, web contract y native adapter con mocks; no afirmar cobertura de substrings accidentales arbitrarios.
- [ ] 6.3 Ejecutar regresión local completa aplicable: Biblioteca usable con credencial ausente y store fallido, detalle local, SavedTitle, personal rating, pins, tags, notes, status, backup, sorting, browsing views, imágenes públicas y todas las verificaciones focalizadas existentes relevantes.
- [ ] 6.4 **MANUAL — Android/iOS reales:** configurar, reiniciar, cambiar, eliminar y reintentar; verificar SecureStore/Keychain/Keystore sin biometría obligatoria, lifecycle documentado por plataforma, show/hide, teclado, lector de pantalla, narrow viewport y cero exposición del token.
- [ ] 6.5 **MANUAL — web:** validar `localStorage` entre refresh y sesiones, limpieza de datos, storage bloqueado, advertencia visible, show/hide, teclado/accesibilidad, viewport angosto y ausencia de afirmaciones de seguridad equivalentes a native.
- [ ] 6.6 **MANUAL — flujos TMDB:** cubrir no-token→configured con query pendiente, token inválido, network/TMDB temporal, retry, remove desde configured, detalle remoto amistoso, carga atómica preservada, links Obtener token/TMDB y Credits/logo/notice.
- [ ] 6.7 Revisar el diff contra invariantes: ningún cambio de SQLite/schema/backup/SavedTitle/rating/pins/tags/notes/status/sorting/views/theme, ninguna cuenta/backend/proxy/Supabase/sessionStorage/cifrado falso/fallback `EXPO_PUBLIC_TMDB_TOKEN`, y sólo la dependencia/configuración autorizada.
- [ ] 6.8 Ejecutar todos los harnesses relevantes, `npx.cmd tsc --noEmit`, `npx.cmd openspec validate add-user-tmdb-credential --strict`, `npx.cmd openspec validate --all --strict` y `git diff --check`; registrar salida exacta y resolver sólo regresiones atribuibles al change.
- [ ] 6.9 **EXTERNAL DIFF REVIEW:** identificar el commit/base real donde comienza `add-user-tmdb-credential` sin asumir `origin/main...HEAD`; revisar el diff completo por scope creep, seguridad/leakage del token, logs/errors, backup exclusion, local-first, Search absent credential, boundary SecureStore/localStorage, mutations/concurrencia, `app.json`/`package.json` y lockfile, dependencias, accesibilidad, attribution, detalle atómico y regresiones; resolver todos los findings antes de considerar completo el change.
- [ ] 6.10 **STOP / REVIEW FINAL:** presentar resultados exactos automatizados/manuales, findings y resoluciones de external review, riesgos residuales, diff final, estado real de todos los checkboxes y confirmación de no commit/push; detenerse antes de proponer o ejecutar archive.
